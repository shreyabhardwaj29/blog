<h1> Welcome To Blog </h1>
<% if (user_signed_in?) %>
<% @articles.each_with_index do |article,index| %>
  <label> Article title: </label>
  <%= article.title %>-- <%= Category.find(article.category_id).name %> </b> <br/>
  <label> Article body:</label>
  <%= article.body %><br/>
  <label> Article published date :</label>
  <%= article.publish_date %><br/>
  <label> Article is published?: </label>
  <% if article.is_published %>
    Article is published
  <% else %>
    Article is not published
  <% end %>
  <br/>
  <label> Image</label>
  <%= image_tag(article.feature_image_url.to_s,:size => "50x50" ,alt: "Image") %><br/><br/>
  
  <% end %>
<%= link_to "Add Articles", new_article_path %>
<% else %>
<label>Register First! </label>
<% end %>
